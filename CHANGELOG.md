# Changelog

## 0.1.0
- Initial release.
- Zero-dependency YAML-subset parser and frontmatter typechecker.
- Base + per-type schema model (`schema.yaml`).
- `check`, `stats`, `types` commands; `--fix`, `--strict`, `--json`, `--quiet`.
- Test suite and CI across Python 3.9 / 3.11 / 3.13.
